{
  "servers": {
    "firebase": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "firebase-tools@latest", "experimental:mcp"],
      "env": {
        // Ensure these env vars exist in your shell or .env.local if needed by firebase auth
      }
    },"github": {
      "url": "https://api.githubcopilot.com/mcp/"
    }
  }
}
